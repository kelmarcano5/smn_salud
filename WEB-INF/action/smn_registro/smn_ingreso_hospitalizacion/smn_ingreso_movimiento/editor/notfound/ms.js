var doc = 0;
$('#doc').val(doc);
//alert("dentro del not found: "+doc);

if (doc == 0) {
	//realiza el insert normal 
	console.log("no se consiguio datos");
	var inserc = 1;
	$('#inserc').val(inserc);
}
